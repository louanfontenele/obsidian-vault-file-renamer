{
	"name": "vault-file-renamer",
	"version": "1.2.0",
	"description": "Standardizes names in Obsidian: lowercase, accents removed, spaces become hyphens, and invalid characters are converted. Includes toggle, target extensions, excluded extensions, and folder blacklist.",
	"main": "main.js",
	"scripts": {
		"build": "node esbuild.config.mjs",
		"watch": "node esbuild.config.mjs"
	},
	"devDependencies": {
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.25.0",
		"eslint": "^9.39.2",
		"eslint-plugin-obsidianmd": "^0.1.9",
		"obsidian": "^1.11.0"
	}
}
